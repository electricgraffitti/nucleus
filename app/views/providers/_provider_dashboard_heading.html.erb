<h3><span><%= link_to "#{provider.full_name}", provider_path(provider) %></span> - Total Billed: <%= number_to_currency(provider.provider_stat.total_billed_count) %></h3>
<div id="provider_rating" class="<%= provider.score_color(provider)%>_rating"><%= link_to "#{provider.provider_score}", provider_action_path %></div>